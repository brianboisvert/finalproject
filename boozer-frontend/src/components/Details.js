import React from 'react';

const Details = (props) => {

  // const cocktails = props.cocktails.map( (c) => (
  //   <li>{c.name}</li>
  // ))

  // const cocktail = props.cocktails.find(el => el.id === props.selected )


  // console.log(props.cocktails[0] ? props.cocktails[0].id : null)
  console.log(props.cocktails[0] ? props.cocktails[0].id : null)

  return(
    <div>
      <h1>{"hey!"}</h1>
    </div>
  )
}

export default Details;
